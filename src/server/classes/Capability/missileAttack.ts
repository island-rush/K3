import { PieceType } from '../../../types';

export const insertMissileAttack = async (gameId: number, missilePiece: PieceType, targetPiece: PieceType) => {
    console.log(gameId);
    console.log(missilePiece);
    console.log(targetPiece);
    return false;
};
